주제 : 드론이 4대가 결합했을때 1대 2대 3대 4대 일때 각각의 제어 로직이나 내부 변수 요소들을 각각이 가지고 있어야함

조건 :

1. 코드 확장성이 좋아야할 것
2. 몇대가 붙었는지 어디로 붙었는지의 정보를 가져와서 선택적으로 객체를 생성할 수 있을 것
3. 일단은 중앙집중방식으로 하여 전체 정보를 메인드론이 수집하게 되는데 독립적인 비행을 할때에는 각각의 
저장 루트에 데이터를 저장하고 결합할 때는 결합모드의 데이터만 따로 저장하는 코드 (but일단은 제쳐둔다.)
4. ROS상에서 구현될 것
5. 코드 블럭이 너무 커지면 이해하기도 어렵고 헷갈리고 각각을 헤더파일로 모듈화한다. --> 헤더 및 클래스로 구현한다.

코드순서 :

1. 변수를 바꾸는 기능들은 모두 함수로 구현한다.
2. Eigen vector, matrix로 모두 구현한다.

3. serial 통신 코드를 업데이트 하거나 실행할떄는 반드시 devel 폴더에 setup.bash를 추가로 해줘야함
4. USB모듈 끼리도 통신이 가능하다?
5. alpha factor, kill/arm, tilt onoff 까지 전달되야함
5.1 세개의 데이터를 모두 string으로변환하여 한줄로 전달, 구분은 ','을 찾으면 거기서 부터 다음 ','까지를
한칸으로 생각하고 데이터를 변환


<--- general module combined code tree stratage --->

1.  단독 비행 모드와 결합비행 모드를 전환하는 함수
1.1 결합 비행 모드시 결합 위치에 따른 내부 파라미터를 업데이트 하는 함수 --- 모듈중 내가 메인인지 서브인지 나누는 구간 필요
1.2 제어인풋을 넣는 구간
1.3 제어인풋을 받아서 제어기에 들어가는 구간.
1.4 

2.  
2.1 
